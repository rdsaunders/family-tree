---
title: "Photos"
layout: base
pagination:
  data: collections.mediaByTitle
  reverse: false
  size: 20
eleventyNavigation:
  key: Photos
---

<div class="container">
    <h1 class="page-title">{{ title }}</h1>

<ul class="grid--masonry">
    {% for item in pagination.items %}
        <li>
          <div class="card">

            <img class="card__img" loading="lazy" srcset="{{ site.cloudinary_url }}image/upload/w_480,f_auto/family_tree{{ item.data.images[0] }} 480w,
                {{ meta.cloudinary_url }}image/upload/w_512,f_auto/family_tree{{ item.data.images[0] }} 512w,
                {{ meta.cloudinary_url }}image/upload/w_768,f_auto/family_tree{{ item.data.images[0] }} 768w,
                {{ meta.cloudinary_url }}image/upload/w_1400,f_auto/family_tree{{ item.data.images[0] }} 1400w"
            src="{{ meta.cloudinary_url }}image/upload/w_320,f_auto/family_tree{{ item.data.images[0] }}" />

            <div class="card__content">
              <div class="card__heading"><a href="{{ item.url }}">
              {% if item.data.caption %}
                {{ item.data.caption }}
              {% else %}
                {{ item.data.title }}
              {% endif %}
              </a>
              </div>
              <p class="card__description">{{ item.data.description }}</p>
            </div>
          </div>
        </li>
    {% endfor %}
</ul>
<div class="u-align-center u-mt2 u-mb2">
{% include "pagination.njk" %}
</div>
</div>

